---
const { bg, id, title } = Astro.props;
---
<section class:list={["px-10 w-fit max-w-prose lg:prose-lg place-self-center", bg || 'bg-background']}} id={id}>
    {title && <h2 class="text-2xl font-bold">{title}</h2>}
    <slot />
</section>