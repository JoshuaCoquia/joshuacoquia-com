---
const { bg, id, title } = Astro.props;
---
<section class:list={["px-10 xl:px-60 py-10 xl:py-20", bg || 'bg-background']}} id={id}>
    <h3 class="text-2xl font-bold">{title}</h3>
    <slot />
</section>