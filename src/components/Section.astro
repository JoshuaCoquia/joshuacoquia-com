---
const { bg, id, title, notprose } = Astro.props;
---
<section class:list={["px-10 w-fit place-self-center", bg || 'bg-background', (!notprose ? 'max-w-prose lg:prose-lg' : '')]}} id={id}>
    {title && <h2 class="text-2xl font-bold">{title}</h2>}
    <slot />
</section>